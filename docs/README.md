# Endless Architecture Docs

## How to Use These Docs

These docs are the **source of truth** for system architecture. When building new features or modifying existing systems:

1. **Before coding**: Read the relevant doc to understand the current architecture, data flow, and known issues.
   For hot-path changes (per-frame/per-tick code), also apply [performance.md](performance.md).
   Hot-path default: Bevy `Query` for filtered scans, `EntityMap` for keyed lookups/indexes.
2. **During coding**: Follow the patterns documented here (job-as-template spawn, chained combat, GPU buffer layout). If you need to deviate, update the doc first.
3. **After coding**: Update the doc if the architecture changed. Add new known issues discovered during implementation.
4. **Code comments** stay educational (explain what code does for someone learning Rust/WGSL). Architecture diagrams, data flow, buffer layouts, and system interaction docs live here, not in code.
5. **README.md** is the game intro (description, gameplay, controls). **docs/roadmap.md** has stages (priority order) and capabilities (feature inventory) — read its maintenance guide before editing.
6. **Write docs in present tense**: explain what the system currently does. Put historical change logs ("was/used to/fixed") in **completed.md** or **CHANGELOG.md**, not architecture docs.

## Test Framework

UI-selectable integration tests run inside the full Bevy app via a bevy_egui menu. From `AppState::MainMenu`, click "Debug Tests" to enter `AppState::TestMenu` which shows the test list; clicking a test transitions to `AppState::Running` where game systems execute normally. "Back to Menu" returns to MainMenu. Tests use **phased assertions** — each phase checks one pipeline layer and fails fast with diagnostic values.

**Architecture** (`rust/src/tests/`):
- `TestState` resource: shared phase tracking, counters, flags, results
- `TestRegistry`: registered test entries (name, description, phase_count, time_scale)
- `TestSetupParams`: SystemParam bundle for test setup (slot alloc, spawn, world data, food, factions, game time, test state, commands, gpu_updates)
- `test_is("name")` run condition gates per-test setup/tick systems
- Each test exports `setup` (OnEnter Running) + `tick` (FixedUpdate after Behavior)
- Helpers: `tick_elapsed()`, `require_entity()` reduce boilerplate
- Cleanup on OnExit(Running): shared `game_cleanup_system` (same as OnExit Playing) — despawn all entities, reset all resources
- Run All: sequential execution via `RunAllState` queue (auto-advances after 1.5s)
- Single tests stay running after pass/fail — user clicks Back in HUD to return

**HUD**: Phase checklist overlay during test execution — gray `○` pending, yellow `▶` active, green `✓` passed, red `✗` failed. Back/Cancel button at bottom.

**Tests** (`src/tests/`):

| Test | Phases | What it validates |
|------|--------|-------------------|
| `vertical-slice` | 8 | Full core loop: spawn → work → raid → combat → death → respawn |
| `spawning` | 4 | Spawn entities, kill via health=0, slot freed, slot reused |
| `energy` | 3 | Energy starts at 100, drains over time, reaches ENERGY_HUNGRY |
| `movement` | 3 | Transit activity set, GPU positions update, AtDestination on arrival |
| `archer-patrol` | 5 | OnDuty → Patrolling → OnDuty → rest when tired → resume |
| `farmer-cycle` | 5 | GoingToWork → Working → tired → rest → recover → return |
| `raider-cycle` | 5 | Dispatch group → arrive at farm → steal → return → deliver |
| `combat` | 6 | GPU targeting → Fighting → damage → health drop → death → slot freed |
| `projectiles` | 4 | Ranged targeting → projectile spawn → hit + damage → slot freed |
| `healing` | 3 | Damaged NPC near town → Healing marker → health recovers to max |
| `economy` | 5 | Farm growing → ready → harvest → raider forage → tent spawner respawn |
| `world-gen` | 6 | Grid dimensions, town placement, buildings, terrain, raider towns |
| `sleep-visual` | 3 | Resting NPC gets sleep icon (atlas_id=3.0) on status layer, cleared on wake |
| `farm-visual` | 3 | Ready farm spawns FarmReadyMarker, cleared on harvest |
| `heal-visual` | 3 | Healing NPC gets halo (atlas_id=2.0) on healing layer, cleared when healed |
| `npc-visuals` | 1 | Visual showcase: all NPC types in labeled grid with individual layer breakdown |
| `terrain-visual` | 1 | Visual showcase: all terrain biomes and building types in labeled grid |
| `friendly-fire-buildings` | 4 | Ranged shooter fires through friendly farm wall without damaging same-faction buildings |
| `endless-mode` | 14 | Builder + raider fountain destroyed → spawn queued → boat migration → settle (both AI kinds) |
| `ai-building` | 2 | AI town building observation: pick personality, watch it build with 100K food+gold on a 10000x10000 map |
| `miner-cycle` | 5 | Miner: walk to mine → tend → harvest gold → deliver → rest |
| `archer-tent-reliability` | 5 | Archer vs enemy tent: target lock, projectile activity, sustained tent damage, destruction |
| `slot-reuse-wave` | 5 | AI wave vs destroyed building: EntityUid prevents ABA slot reuse (wave correctly ends) |
| `coalesce-movement` | 2 | GPU-authoritative position safety: SetPosition on foreign slot doesn't teleport other NPCs |
| `coalesce-arrival` | 2 | GPU-authoritative arrival safety: arrived NPC stays stable after unrelated SetTarget |
| `pathfind-maze` | 5 | Farmer navigates serpentine wall maze via A* pathfinding (spawn → waypoints → cross walls → reach farm) |
| `sandbox` | 1 | Human player sandbox: 1 player + 1 AI town, 100K food+gold, no raiders — auto-completes for free play |

## System Map

```
Pure Bevy App (main.rs)
    │
    ▼
Bevy ECS (lib.rs build_app)
    │
    ├─ AppState: MainMenu → Playing | TestMenu → Running
    │
    ├─ UI (ui/) ─────────────────────────────▶ main_menu, game_hud, panels, startup, cleanup
    │   ├─ Main menu: sectioned layout (World / Difficulty / Options / Debug), Continents-only world generation (no worldgen selector), player-focused per-town caps (farms/mines/farmer homes/archer homes up to 10), non-player homes (miner/fighter/crossbow) removed from player-facing sliders, endless mode always enabled (no toggle), raider passive forage under raider towns, tooltips on controls, uniform button spacing, Play / Load Game / Settings / Debug Tests / Exit
    │   ├─ Game startup: world gen + NPC spawn (OnEnter Playing)
    │   ├─ Top bar: panel toggles left, town name + time center, stats (food + gold) + UPS + FPS right
    │   ├─ Floating windows: NPC/building/DC-group inspector with combat stats + equipment + status + keep-fighting toggle (bottom-left) + combat log with faction filter + type filters (bottom-right); game over screen (fountain destroyed → dimmed overlay + stats + Play Again/Keep Watching/Exit)
    │   ├─ Casino popup: blackjack minigame window (double-click Casino building or keybind), gold wagering + reputation
    │   ├─ Left panel: floating Window with Roster (R) / Upgrades (U) / Policies (P) / Patrols (T) / Squads (Q) / Inventory (I) / Factions (G) / Help (H)
    │   ├─ Jukebox overlay: top-right, track picker dropdown + pause/skip/loop/speed controls
    │   ├─ Build menu: bottom-center horizontal bar with Economy/Military/Tower tabs; building sprites + help text + hover tooltips; click-to-place with grid-snapped ghost preview; Road drag-line placement; Wall placement (town grid, blocks enemy NPCs); Tower placement (town grid, auto-shoots enemies); destroy mode in bar + inspector
    │   ├─ Tutorial: 24-step guided walkthrough (camera → building → NPC interaction → walls → upgrades → roads → policies → patrols → squads → save/load → controls); dynamic keybinding text via key_label_for_action(); condition-driven auto-advance + manual Next/Skip + 10-min auto-end
    │   ├─ Pause menu (ESC): Resume, tabbed Settings panel (PauseSettingsTab: Interface/Video/Camera/Controls/Audio/Logs/Debug/SaveGame/LoadGame) with scrollable content, game quicksave/quickload + manual named save/load (named_save_path), separate settings-file save/reload/reset controls, Exit to Main Menu — available in both Playing and Running (test scenes)
    │   └─ Game cleanup: despawn + reset (shared by OnExit Playing + OnExit Running)
    │
    ├─ Messages (static queues) ───────────▶ [messages.md]
    │
    ├─ GPU Compute (gpu.rs) ───────────────▶ [gpu-compute.md]
    │   ├─ Bevy render graph integration
    │   └─ WGSL shader (shaders/npc_compute.wgsl)
    │
    ├─ NPC Rendering (npc_render.rs) ─────────▶ [rendering.md]
    │   ├─ RenderCommand + Transparent2d phase, explicit sort-key ordering
    │   ├─ Storage buffer path (NPCs + selection brackets): 3 shader-def variants via StorageDrawMode
    │   ├─ Instance buffer path (building bodies, building overlays, projectiles)
    │   └─ WGSL shader (shaders/npc_render.wgsl)
    │
    ├─ Sprite Rendering (render.rs) ───────────▶ [rendering.md]
    │   ├─ 2D camera, texture atlases
    │   └─ Character + world + extras (heal/sleep/arrow/boat) sprite sheets
    │
    ├─ Bevy Systems (FixedUpdate 60 Hz, gated on AppState::Playing | Running)
    │   ├─ Spawn systems ──────────────────▶ [spawn.md]
    │   ├─ Combat pipeline ────────────────▶ [combat.md]
    │   ├─ Behavior systems ───────────────▶ [behavior.md]
    │   ├─ Economy systems ────────────────▶ [economy.md]
    │   ├─ AI player system ─────────────▶ [ai-player.md]
    │   └─ Audio (systems/audio.rs) ───▶ music jukebox (22 tracks, random no-repeat, speed control), SFX (ArrowShoot + Death, random variant selection, spatial camera culling, max 1 per SfxKind per frame, ArrowShoot gated by `sfx_shoot_enabled`)
    │
    └─ Test Framework (tests/)
        ├─ bevy_egui menu (EguiPrimaryContextPass)
        ├─ AppState: TestMenu ↔ Running
        └─ Per-test setup + tick systems

Frame execution order ─────────────────────▶ [frame-loop.md]
```

## Documents

| Doc | What it covers | Rating |
|-----|---------------|--------|
| [frame-loop.md](frame-loop.md) | Fixed 60 UPS game loop, FixedUpdate/Update split, main/render world timing | 9/10 |
| [gpu-compute.md](gpu-compute.md) | Compute shaders, spatial grid, separation physics, combat targeting, GPU→ECS readback | 8/10 |
| [rendering.md](rendering.md) | TilemapChunk terrain, GPU instanced buildings/NPCs/equipment, 4-atlas pipeline (char/world/extras/building), explicit sort-key pass ordering, RenderCommand pattern, camera controls, health bars | 8/10 |
| [combat.md](combat.md) | Attack → damage → death → XP grant → cleanup, slot recycling | 8/10 |
| [spawn.md](spawn.md) | Single spawn path, job-as-template, slot allocation, DRY save-load via materialize_npc | 8/10 |
| [behavior.md](behavior.md) | Decision system, utility AI, state machine, energy, patrol, flee/leash (bucketing formulas → performance.md) | 8/10 |
| [ai-player.md](ai-player.md) | AI decision loop, hunger system, building scoring, slot placement, squad commander, migration | 8/10 |
| [economy.md](economy.md) | Farm growth, food theft, starvation, raider foraging, spawner respawn (BuildingInstance fields), dynamic raider town migration (spawn→boat→disembark→walk→settle) | 8/10 |
| [messages.md](messages.md) | Message flow, GpuUpdateMsg, GAME_CONFIG_STAGING, readback resources (authority → [authority.md](authority.md)) | 7/10 |
| [resources.md](resources.md) | Bevy resources, game state ownership, UI caches, world data | 7/10 |
| [projectiles.md](projectiles.md) | GPU projectile compute, hit detection, instanced rendering, slot allocation | 7/10 |
| [authority.md](authority.md) | Complete data ownership (GPU/CPU/render-only), hard rules, staleness budget, slot namespace | 9/10 |
| [performance.md](performance.md) | Complete perf authority: GPU patterns, CPU cadencing, data access rules, anti-patterns, PR review | 9/10 |
| [concepts.md](concepts.md) | Foundational patterns (DOD, spatial grid, compute shaders, ECS) | - |
| [roadmap.md](roadmap.md) | Feature tracking, migration plan | - |

Ratings reflect system quality, not doc accuracy.

## File Map

```
rust/
  Cargo.toml            # Pure Bevy 0.18 + bevy_egui + bytemuck + rand + noise + bevy_embedded_assets
  src/main.rs           # Bevy App entry point, EmbeddedAssetPlugin (release), maximize window on startup, crash handler (panic hook → clipboard + crash.log + native dialog)
  src/lib.rs            # build_app(), AppState enum, system scheduling (FixedUpdate 60 Hz game loop + Update for UI/save/audio), ups_tick system, helpers
  src/tracing_layer.rs  # Custom tracing Layer (SystemTimingLayer) that captures Bevy per-system span durations into TRACING_TIMINGS global map; EMA-smoothed (0.9/0.1), runtime-toggled via RENDER_PROFILING AtomicBool; used by frame_timer_start to feed profiler UI
  src/gpu.rs            # GPU compute via Bevy render graph, RenderFrameConfig (single ExtractResource bundling EntityGpuData + ProjGpuData + NpcSpriteTexture (extras_handle for consolidated atlas) + ReadbackHandles + tile_flags), EntityGpuBuffers (unified entity buffers sized to MAX_ENTITIES=200K), EntityGpuState (unified CPU-side state for all entities: positions/factions/healths/max_healths/entity_flags/sprites/flash/targets/speeds/arrivals, default speeds=0.0 so uninitialized entities don't move + per-index dirty tracking with authority contract: positions/arrivals are GPU-authoritative (strict coalescing only), all others CPU-authoritative (gap-based coalescing safe); dirty indices pre-sorted+deduped in populate_gpu_state; visual_dirty_indices for full visual+equip updates, flash_only_indices for flash-decay-only visual updates (no equip), visual_full_rebuild for startup/load; healths stored normalized 0.0-1.0, SetHealth/ApplyDamage divide by max_healths[idx], SetMaxHealth updates max_healths), populate_gpu_state: drains GpuSlotPool.pending_frees (hide cleanup) then pending_resets (full GPU state wipe) before routing GpuUpdate variants to EntityGpuState (Hide clears sprite_indices+flash, pushes hidden_indices+visual_dirty; SetSpriteFrame/SetDamageFlash/MarkVisualDirty push visual_dirty; flash decay → flash_only_indices; post-processing: sort_unstable+dedup on all dirty index Vecs), build_visual_upload: event-driven dirty-slot-only update of persistent NpcVisualUpload (sizes buffers from live GpuSlotPool.count() — not stale RenderFrameConfig copy; full rebuild on startup/load, query-first ECS iteration for full path, EntityMap lookup for dirty path, flash-only path updates just flash float via binary_search dedup, stale slots cleared to sentinels, populates visual_uploaded_indices + equip_uploaded_indices for coalesced GPU upload in extract phase), populate_tile_flags system (terrain bitfield from WorldGrid + building flags from EntityMap iteration, encodes wall faction in bits 8-11)
  src/npc_render.rs     # GPU NPC rendering (storage buffers) + building body/overlay/projectile/selection bracket rendering (instance buffers), SelectionInstance (slot/color/scale/y_offset) + SelectionOverlayInstances built from SelectedNpc/SelectedBuilding/DirectControl, SelectionRenderBuffers + DrawSelectionBrackets RenderCommand (storage+instance hybrid: positions from npc_positions[slot], style from instance buffer), BuildingBodyInstances from EntityGpuState via EntityMap.iter_instances(), extract_npc_data uploads EntityGpuState to EntityGpuBuffers via two coalescing strategies: strict coalescing (write_coalesced_exact_f32/i32) for GPU-authoritative buffers (positions, arrivals) — merges only exactly-adjacent dirty indices with saturating_add(1), no gap merging, debug-asserted sorted+deduped+bounds; gap-based coalescing (write_coalesced_f32/i32/u32) for CPU-authoritative buffers (targets, speeds, factions, healths, flags, half_sizes) — per-stride gap thresholds (GAP_STRIDE_1=750, GAP_STRIDE_2=375, GAP_VISUAL=750, GAP_EQUIP=27, max ~24KB wasted/gap), 40% window fallback to bulk offset write; count_exact_ranges + count_gap_ranges profiler helpers track coalescing write counts + bytes; visual coalesced via visual_uploaded_indices, equip coalesced via equip_uploaded_indices (excludes flash-only slots)
  src/render.rs         # 2D camera, texture atlases (SpriteAssets with registry-driven external_textures Vec), TilemapChunk spawning (terrain only), TerrainChunk sync, building atlas + extras atlas creation (build_extras_atlas), click_to_select_system (ClickSelectParams SystemParam, double-click fountain → Factions tab, double-click casino → blackjack popup, NPC selection via GPU readback positions, building selection via authoritative EntityMap positions (not GPU readback — deterministic placement), dead NPC guard via EntityMap.entities, right-click: placing_target mode sets squad.target, default mode commands DirectControl NPCs only with manual_target + MovementIntents.submit(DirectControl) + wakes Resting/GoingToRest NPCs to Idle), box_select_system (drag-rectangle multi-select via query-first `(&GpuSlot, &Job, &Faction, &Position)` with `Without<Building>, Without<Dead>`, sets NpcFlags.direct_control + inserts SquadId via Commands, HashSet for O(1) membership checks, clears SelectedNpc + SelectedBuilding so inspector shows DC group view)
  src/messages.rs       # Static queues (GpuUpdate — unified variants for NPCs+buildings incl. MarkVisualDirty + SetMaxHealth for health normalization, no Bld* variants), Message types (DamageMsg unified NPC+building, target: EntityUid for stable identity), DirtyWriters SystemParam (dirty signal messages), CombatLogMsg, WorkIntentMsg/WorkIntent enum (Claim/Release/Retarget for centralized work targeting)
  src/components.rs     # ECS components for all entities: buildings (GpuSlot, Position, Health, Faction, TownId, Building, FarmReadyMarker, Dead, LastHitBy) + NPC components (Job, Activity (with visual_key() for dirty tracking), CombatState, Energy, Speed, Home, Personality, CachedStats (incl. stamina drain multiplier + hp_regen + berserk_bonus), BaseAttackType, AttackTimer, ManualTarget, PatrolRoute, NpcWorkState (always-present: worksite as Option\<EntityUid\>, single field — all mutations via WorkIntentMsg), CarriedGold, EquippedWeapon/Helmet/Armor, FleeThreshold, LeashRange, WoundedThreshold, NpcFlags, SquadId, Stealer, HasEnergy — all derive Component), TraitKind (7-axis spectrum: Courage/Diligence/Vitality/Power/Agility/Precision/Ferocity) + TraitInstance (signed magnitude ±0.5-1.5), TraitStatMods/TraitBehaviorMods structs, Personality.get_stat_mods()/get_behavior_mods() for stat and behavior modification
  src/constants.rs      # Tuning parameters (grid size, separation, energy rates, TowerStats struct (range/damage/cooldown/proj_speed/proj_lifetime/hp_regen/max_hp) + FOUNTAIN_TOWER/TOWER_STATS consts, squad limits, mining, MAX_MINE_OCCUPANCY, 8x8 base build area, WAYPOINT_COVER_RADIUS, MAX_NPC_COUNT=100K, MAX_BUILDINGS=100K, MAX_ENTITIES=200K, ENTITY_FLAG_COMBAT/ENTITY_FLAG_BUILDING/ENTITY_FLAG_UNTARGETABLE, ATLAS_* IDs, BUILDING_CONSTRUCT_SECS=10.0), NPC_REGISTRY (single source of truth: NpcDef with job/label/label_plural/sprite/atlas/color/ui_color/stats/attack_override/classification flags/spawn component flags/home_building/is_raider_unit/default_count/upgrade_category/upgrade_stats/loot_drop slice — atlas field selects sprite atlas: 0.0=character, ATLAS_BOAT=boat), npc_def(job), UpgradeStatKind enum, UpgradeStatDef struct (kind/pct/cost/label/tooltip/display/prereqs/flags), EffectDisplay enum, const upgrade arrays (MILITARY_RANGED_UPGRADES/MILITARY_MELEE_UPGRADES/FARMER_UPGRADES/MINER_UPGRADES/TOWN_UPGRADES — all include Stamina upgrade with MoveSpeed prereq; military arrays include HpRegen), TOWER_UPGRADES (HP/Attack/Range/AtkSpd/ProjSpd/ProjLife/HpRegen — per-instance tower upgrades), BUILDING_REGISTRY (16 BuildingDef entries with kind/display(DisplayCategory)/tile/hp/cost/label/tooltip/spawner/placement/tower_stats/save_key/is_unit_home/worksite/autotile — pure static definitions, no fn pointers), WorksiteDef (max_occupants/drift_radius/upgrade_job/harvest_item/town_scoped — Farm=1 occupant+town-scoped, GoldMine=5+global), BuildingDef::loot_drop() method (derives cost/2 as food), building_def(kind), tileset_index(kind), building_cost(kind), TileSpec, SpawnerDef, SpawnBehavior, PlacementMode, OnPlace, DisplayCategory (Hidden/Economy/Military/Tower), TILE_* bitfield constants (terrain: GRASS/FOREST/WATER/ROCK/DIRT bits 0-4, building: ROAD bit 5, WALL bit 6 + faction bits 8-11), ROAD_SPEED_MULT, WALL_TIER_HP/WALL_TIER_NAMES/WALL_UPGRADE_COSTS (3-tier wall progression), Road is player_buildable + raider_buildable, AUTOTILE_EXTRA_PER_KIND (10 variant layers per kind), AUTOTILE_* variant indices, autotile_col/autotile_order/autotile_kind_count/autotile_total_extra_layers helpers
  src/resources.rs      # Bevy resources (PauseSettingsTab enum (Interface/Video/Camera/Controls/Audio/Logs/Debug/SaveGame/LoadGame with label()/title_subtitle() methods) in UiState, SlotPool shared inner type, GpuSlotPool wraps SlotPool with pending_resets + pending_frees lifecycle queues, alloc_reset() allocates + queues GPU state wipe, free() deallocates + queues GPU hide, max=MAX_ENTITIES=200K (unified NPC+building namespace), EntityMap: unified entities HashMap<usize,Entity> + npcs: HashMap<usize,NpcEntry> (6-field index: slot, entity, job, faction, town_idx, dead — all gameplay state lives in ECS components; register_npc/unregister_npc for index management) + npc_by_town secondary index + UID maps (uid_to_slot/slot_to_uid/uid_to_entity/entity_to_uid for stable EntityUid identity) + building instance data/spatial grid/indexes (sole source of truth for all slot→entity lookups AND building instances; BuildingInstance has npc_uid: Option\<EntityUid\>/respawn_timer for spawner state + occupants: i16 for slot-indexed occupancy + under_construction: f32 seconds remaining (0.0=complete) + kills: i32 + xp: i32 (tower/fountain kill tracking, same +100/kill scale as NPCs) + upgrade_levels: Vec\<u8\> + auto_upgrade_flags: Vec<bool> (per-tower upgrades); grid-coord accessors: has_building_at(gc,gr)/get_at_grid(gc,gr); occupancy methods: release/occupant_count/is_occupied/slot_at_position; kind-filtered spatial: spatial_kind_town + spatial_kind_cell per-cell buckets with SpatialBucketRef back-index for O(1) swap-remove, for_each_nearby_kind_town/for_each_nearby_kind/for_each_ring_kind_town/for_each_ring_kind cell-ring queries, find_nearest_worksite (min-order tuple scoring + WorksiteFallback), try_claim_worksite (authoritative validate+claim)), GameTime, FactionStats, TowerState (town: TowerKindState for fountains, tower_cooldowns: HashMap<usize,f32> for player-built towers)/TowerKindState, SquadState/SquadOwner (Squad has members: Vec\<EntityUid\>, wave_active/wave_start_count/wave_min_start/wave_retreat_below_pct/hold_fire), SquadState has drag_start/box_selecting for box-select, dc_no_return for DC keep-fighting toggle), GoldStorage, MineStates, MinerProgressRender, MiningPolicy (mine_enabled keyed by GPU slot), SelectedBuilding (kind + slot), BuildMenuContext (selected_build: Option<BuildingKind>, destroy_mode: bool, build_tab: DisplayCategory), UiState.game_over (player fountain destroyed → defeat screen), UiState.tower_upgrade_slot (Option<usize> for tower upgrade popup window), UiState.casino_open (bool for blackjack popup), BuildingHealState (persistent needs_healing flag for building healing), HelpCatalog, DifficultyPreset (raider_forage_hours per difficulty: Easy=12, Normal=6, Hard=3), GameConfig, MovementIntents (HashMap<Entity, MovementIntent> with MovementPriority arbitration — sole source of NPC movement targeting), GameAudio (sfx_handles: HashMap<SfxKind, Vec<Handle<AudioSource>>> for random variant selection), SfxKind enum (ArrowShoot/Death/Build/Click/Upgrade), PlaySfxMsg (kind + position for spatial culling), UpsCounter (ticks_this_second incremented in FixedUpdate, display_ups sampled by HUD per wall-clock second), etc.)
  src/save.rs            # Save/load system (default quicksave/quickload bindings F5/F9 via UserSettings key bindings, autosave with 3 rotating slots, save file picker via list_saves/read_save_from, named_save_path (sanitized filename → Documents/Endless/saves/<name>.json), SaveLoadRequest.save_path for named saves + write_save_to, SaveData serialization (terrain-only grid cells + original_terrain for terrain restoration on fountain death, building instances serialized from EntityMap as Vec<PlacedBuilding> per save_key for backward compat, building HP as HashMap<String, Vec<f32>> collected from entity Health, NPC data collected via SaveNpcQueries SystemParam from ECS queries including NpcWorkState for worksite, UID fields: NpcSaveData.uid, SpawnerSave.npc_uid, SquadSave.member_uids, SaveData.next_entity_uid — PersonalitySave with version field (0=legacy 4-trait, 1=spectrum 7-axis) + from_legacy_id migration, all #[serde(default)] for old-save compat, AiPlayerSave with build_enabled/upgrade_enabled fields for Player AI Manager, PlacedBuilding kills/xp/upgrade_levels/auto_upgrade_flags fields for tower kill tracking + per-tower upgrades), SAVE_VERSION with version-gated migration in apply_save, spawn_npcs_from_save delegates to materialize_npc() via NpcSpawnOverrides (uid_override), restore_world_from_save() centralizes load-restore path (takes &mut EntityMap separately, uses place_building with hp_override for building restoration, update_all_autotile for all autotile-enabled kinds, includes post-spawn squad member UID fixup for old saves, forces endless mode enabled on load))
  src/settings.rs       # UserSettings persistence (serde JSON save/load, version migration v13, SETTINGS_VERSION=13, npc_counts BTreeMap<String,usize> with legacy farmers/archers/raiders migration, key_bindings map (action id -> KeyCode string) with defaults/fallback migration, auto_upgrades, upgrade_expanded (Vec<String> for collapsible branch persistence), autosave_hours, music/sfx volume (sfx default 0.15), music speed, tutorial_completed, log_faction_filter, debug_ai_decisions, interface_text_size (default 16.0, drives global egui text sizing), zoom_speed/zoom_min/zoom_max/lod_transition, fullscreen (borderless, default on), vsync, fps_cap (0=uncapped, otherwise target FPS e.g. 30/60/120/144/240, drives Bevy focused UpdateMode via focused_mode_for_fps_cap), window_width/window_height (default 1920x1080), npc_log_mode (NpcLogMode: All/Faction/SelectedOnly), jukebox_track/jukebox_paused (persist jukebox state across sessions), left_panel_tab (last active tab as string), collapsed_sections (Vec<String> of collapsed collapsing headers), raider_forage_hours (f32, default 6.0 = Normal, hours per 1 food for raider passive foraging, 0 = disabled))
  src/world.rs          # World data structs (PlacedBuilding struct for save/load backward compat only (includes upgrade_levels/auto_upgrade_flags for tower persistence), WorldData contains only towns: Vec<Town>), world grid (WorldCell has terrain: Biome + original_terrain: Biome (pre-stamp_dirt terrain, used to restore on fountain destruction) — no building field, building presence queried via EntityMap), procedural generation (mine placement, building instances created directly via place_building), AiPlayer registration for all towns (faction > 0: active=true with random personality/road; faction 0: active=false, Builder, Balanced, Grid4 — player AI manager), tileset builder (build_tileset for terrain array, build_building_atlas for building strip + auto-tile layers for all autotile-enabled kinds, build_extras_atlas for extras horizontal grid), auto-tile (autotile_variant(entity_map, col, row, kind) kind-agnostic neighbor-based offset selection, update_autotile_around for placement/removal, update_all_autotile for load, extract_sprite_32 + rotate_90_cw helpers), town grid, BuildingKind enum (Fountain/Bed/Waypoint/Farm/FarmerHome/ArcherHome/Tent/GoldMine/MinerHome/CrossbowHome/FighterHome/Road/Wall/Tower/Merchant/Casino), building_tiles() (tile specs from registry), shared helpers: is_alive(), empty_slots(tg, center, grid, building_map), place_building() (unified single entry point for ALL building creation — world-gen, runtime, save/load, migration, tests; BuildContext optional param controls runtime validation: cell checks, cost deduction, construction timer, auto-tile for autotile-enabled kinds, dirty signals; None = free placement at full HP; always: GPU slot alloc, BuildingInstance, ECS entity spawn, GPU updates incl. SetMaxHealth), find_nearest_free(from, entity_map, kind, town_idx) → Option<(usize, Vec2)> (returns slot+position, checks inst.occupants), resolve_spawner_npc() (takes &BuildingInstance, uses SpawnBehavior from registry, returns 9-tuple with work_slot: Option<usize>), destroy_building() (cleanup only: combat log + auto-tile neighbor update; callers send DamageMsg for entity death — single Dead writer is death_system), clear_town_roads_and_dirt() (removes all roads for a town + restores dirt cells to original_terrain, called on fountain destruction)
  src/ui/
    mod.rs              # register_ui(), game startup (+ policy load), cleanup, shared settings_panel_ui() (renders all settings tabs into a ui region, returns SettingsResponse with reset/save/load flags + paths; callers handle side effects — used by both pause menu and main menu; Interface tab includes autosave slider + Restart Tutorial button, Debug tab includes AI Think + NPC Think interval sliders), pause menu (wraps settings_panel_ui with Save/Load tabs + Resume/Exit buttons, PauseMenuLocals struct bundles save_name/load_name/rebinding to stay within Bevy 16-param limit, syncs ai_interval/npc_interval to AiPlayerConfig/NpcDecisionConfig + autosave_hours to SaveLoadRequest live, suppressed when game_over is active), game_over_system (UiState.game_over flag set by death_system when player faction 0 fountain destroyed → pauses game, shows dimmed overlay + "Defeated" window with stats (days survived, NPCs alive/lost, kills, food, gold) + Play Again/Keep Watching/Exit to Main Menu buttons), escape/time controls (Playing + Running states), keyboard toggles (defaults: Q=squads, H=help; configurable via key bindings), casino keybind toggles UiState.casino_open, build ghost preview (Road drag-line with ghost trail), slot indicators, process_destroy_system (resolves exact building slot by kind+town+grid before lethal DamageMsg), apply_ui_scale, apply_interface_text_size (global egui text sizing from UserSettings), test scene systems (bottom panel + target overlay + pause menu in Running state)
    main_menu.rs        # Main menu with sectioned layout (World / Difficulty / Debug Options), Continents-only worldgen (no gen_style selector, always Continents), difficulty presets (Easy/Normal/Hard) drive endless strength + baseline counts (endless mode always enabled, no UI toggle), "Per Town (player & AI)" exposes farms/archer homes only (0..10 caps, strip_disabled_home_jobs removes Miner/Fighter/Crossbow from player sliders), AI Builder Towns + AI Raider Towns sliders with raider forage hours slider nested under raiders (0=off, hours per 1 food, preset-driven: Easy=12h, Normal=6h, Hard=3h), tooltips on controls, uniform button spacing, Play / Load Game / Settings / Debug Tests / Exit; Settings button opens floating window with shared settings_panel_ui (same tabs as pause menu minus Save/Load), side effects applied via pre/post snapshot pattern (video/audio/winit)
    game_hud.rs         # Top bar (food + gold + UPS + FPS), jukebox overlay (track picker + pause/skip/loop/speed), floating inspector with combat stats/equipment/status/carried loot + clickable faction link + clickable home building link via building_link → InspectorAction (bottom-left) + DC group inspector (dc_slots() helper iterates selected squad members O(squad_size) instead of iter_npcs() O(n), unit count/HP/job breakdown/keep-fighting toggle when box-selected) + combat log with faction filter dropdown + clickable ">>" camera-pan on location entries (bottom-right), registry-driven spawner inspector (def.spawner + npc_def for labels, linked NPC state/squad/patrol/position/NpcWorkState via ECS queries, clickable NPC name link via npc_link → InspectorAction → select+camera-jump), wall inspector (tier name/HP bar/upgrade button with cost check + deduction), tower inspector (resolved per-instance stats via resolve_tower_instance_stats + HP bar + kills/xp/level always shown + "Upgrades" button opens tower_upgrade_window popup with per-stat upgrade buttons + per-stat auto-buy checkboxes), construction status (yellow "Under Construction" label + progress bar with time remaining, hides per-type details during construction), mine assignment UI (slot-based via EntityMap building data, named Gold Mine A/B/C via gold_mine_index), target overlay, squad overlay (numbered target circles hidden during box-select, DirectControl crosshair from ManualTarget::Npc/Building queries), BuildingInspectorData SystemParam (19 fields incl. GpuSlotPool + ECS queries for Personality/Home/Equipment/CarriedGold/PatrolRoute), expanded Copy Debug Info (NPC: XP/traits/stats/equipment/NpcFlags dump/Activity debug repr/CombatState/ManualTarget/WorkState/squad/CarriedGold/Returning loot/PatrolRoute/miner details; Building: faction/per-type details + GPU raw state (position/target/health/faction/flags/sprite via EntityGpuState) + slot allocator diagnostics + EntityMap cross-references + selection overlay expected values)
    left_panel.rs       # Tabbed floating Window: Roster (R) / Upgrades (U) / Policies (P) / Patrols (T) / Squads (Q) / Inventory (I) / Factions (G) / Help (H), persisted active tab via left_panel_tab + collapsing section state via collapsed_sections (TRACKED_SECTIONS + snapshot/restore helpers) — Upgrades tab: Economy/Military section headers with collapsible per-NPC branches (Farmer/Miner/Town under Economy, Archer/Fighter/Crossbow under Military), driven by UPGRADES.branches with section field, CollapsingState with expand/collapse persisted to UserSettings.upgrade_expanded (default collapsed); policy persistence on tab leave, Policies tab: General/Archers/Farmers/Mining sections + AI Manager section (enable toggle, auto-build/upgrade toggles, personality picker, road style picker — controls faction 0 AiPlayer via ResMut<AiPlayerState> from FactionsParams), Factions tab shows policy snapshot + squad commander details per faction with per-unit stat breakdowns (Archer/Fighter/Crossbow separate), food/military desire bars via shared `debug_food_military_desire` (same logic as live AI), "Copy Debug" button (builds comprehensive debug string via build_faction_debug_string -> clipboard), registry-driven intel panel (DisplayCategory Economy/Military columns, label_plural from NPC_REGISTRY), registry-driven roster (job filter buttons + row colors from NPC_REGISTRY, military-first ordering, RosterParams SystemParam with Personality query for trait display), registry-driven squad UI (per-job recruit rows from NPC_REGISTRY is_military, job-colored member list, hold-fire checkbox, attack target display), Profiler tab cached via Local<ProfilerCache> (refreshes every 15 frames: mutex locks + HashMap clones + top_offenders scan amortized ~4x/sec, renders top 10 per section instead of all entries)
    blackjack.rs        # Casino blackjack minigame — standalone popup window (UiState.casino_open), 3-deck shoe, hit/stand/double/split, gold wagering + reputation gain, AI faction opponents from WorldData
    build_menu.rs       # Bottom-center build bar: data-driven from BUILDING_REGISTRY (player_buildable/raider_buildable filter), Economy/Military category tabs via DisplayCategory + BuildMenuContext.build_tab (tab switch clears selection if building in other category), 1-per-town limit for Merchant + Casino, cached atlas extraction, click-to-place, destroy mode, cursor hint, hover tooltips from BuildingDef.tooltip
    tutorial.rs         # 20-step guided tutorial: condition-driven hints (action triggers + info-only Next steps), skip per-step or all, 10-minute auto-end timeout, persisted completion in UserSettings
  src/tests/
    mod.rs              # Test framework (TestState, menu UI, HUD, time controls: Space=pause, +/-=speed; cleanup via shared game_cleanup_system from ui/mod.rs)
    vertical_slice.rs   # Full core loop test (8 phases, spawn→combat→death→respawn)
    spawning.rs         # Spawn + slot reuse test (4 phases)
    energy.rs           # Energy drain test (3 phases)
    movement.rs         # Movement + arrival test (3 phases)
    archer_patrol.rs    # Archer patrol cycle (5 phases)
    farmer_cycle.rs     # Farmer work cycle (5 phases)
    raider_cycle.rs     # Raider raid cycle (5 phases)
    combat.rs           # Combat pipeline test (6 phases)
    projectiles.rs      # Projectile pipeline test (4 phases)
    healing.rs          # Healing aura test (3 phases)
    economy.rs          # Economy test (5 phases)
    world_gen.rs        # World generation test (6 phases)
    sleep_visual.rs     # Sleep icon visual test (3 phases)
    farm_visual.rs      # Farm ready marker visual test (3 phases)
    heal_visual.rs      # Heal icon visual test (3 phases)
    npc_visuals.rs    # NPC visual showcase — all types × all layers in labeled grid (1 phase)
    terrain_visual.rs # Terrain + building visual showcase — all biomes and building types (1 phase)
    friendly_fire_buildings.rs # Friendly fire regression — shooter through farm wall (4 phases)
    endless_mode.rs   # Endless mode test — builder + raider fountain destroy → migration → settle (14 phases)
    ai_building.rs    # AI building observation test — pick personality, watch AI build with 100K food+gold on 10000x10000 world (2 phases)
    miner_cycle.rs    # Miner work cycle test (5 phases)
    archer_tent_reliability.rs # Archer vs tent targeting + destruction test (5 phases)
    coalesce_safety.rs # GPU-authoritative buffer coalesce safety (movement: 2 phases, arrival: 2 phases)
    sandbox.rs        # Human player sandbox: 1 player + 1 AI town, 100K food+gold, no raiders, auto-completes for free play
  src/systems/
    spawn.rs            # Spawn system (MessageReader<SpawnNpcMsg>), materialize_npc() shared helper (single source of truth for fresh spawn + save-load, spawns ECS entity with full component set via nested tuple bundles + conditional inserts, registers slot→entity index in EntityMap, assigns EntityUid via uid_override or NextEntityUid, emits SetMaxHealth + SetHealth for per-job HP normalization, uses def.atlas for sprite atlas selection), generate_personality() (deterministic LCG, 7-axis spectrum, 20% per axis, signed magnitude, cap at 2), NpcSpawnOverrides (includes uid_override), build_patrol_route(&EntityMap)
    stats.rs            # CombatConfig (jobs + attacks + crossbow_attack base), TownUpgrades (Vec<Vec<u8>>), UpgradeMsg message flow, dynamic UpgradeRegistry via LazyLock (UpgradeNode/UpgradeBranch with section grouping, built from NPC_REGISTRY upgrade_stats + TOWN_UPGRADES at init), UPGRADES static, resolve_combat_stats() (uses npc_def.upgrade_category + UPGRADES.stat_mult + TraitStatMods for range/cooldown/berserk, includes hp_regen from HpRegen upgrade level), resolve_tower_instance_stats() (per-instance tower stat resolution: XP level bonus +1%/level + per-stat upgrade multipliers from TOWER_UPGRADES), level_from_xp(), process_upgrades_system (reads Personality from ECS query for stat re-resolve on upgrade, emits SetMaxHealth + SetHealth for HP scaling on upgrade), auto_upgrade_system, auto_tower_upgrade_system (game-hour tick: towers with any auto_upgrade_flags set buy cheapest affordable flagged upgrade), upgrade helpers (upgrade_unlocked/upgrade_available/deduct_upgrade_cost/format_upgrade_cost (explicit "N food, N gold" labels)/missing_prereqs/upgrade_effect_summary/branch_total/expansion_cost)
    drain.rs            # Queue drain systems (drain_combat_log collects CombatLogMsg → CombatLog)
    movement.rs         # GPU position readback (writes ECS Position + NpcFlags.at_destination via Query), arrival detection, resolve_movement_system (unified intent resolution + A* path routing: drains world-space intents → enqueues PathRequests → budget-limited LOS bypass or A* routing; sole SetTarget emitter)
    combat.rs           # Attack cooldown, GPU-unified targeting (query-first NPC iteration + EntityMap for building target resolution, NPC target faction from ECS not GPU readback, berserk_bonus applied when HP <50%), fire_projectile (shared helper emits ArrowShoot SFX with shooter position on successful fire), process_proj_hits (unified DamageMsg with EntityUid target, requires Res<EntityMap>), building_tower_system (fountain towers via GPU combat_targets readback at unified slot, shooter=bld_slot for self-collision skip, per-tower resolved stats via resolve_tower_instance_stats, tower HP regen tick for towers with hp_regen > 0)
    health.rs           # Unified damage_system (NPC + building damage via EntityUid→slot resolution then EntityMap routing — get_instance for buildings, entities for NPCs, SetDamageFlash), unified death_system (Phase 1a marks newly dead NPCs via query-first ECS iteration (Health + GpuSlot, Without<Dead>) — no iter_npcs() scan. Inserts Dead marker component, reuses collected slots for Phase 2b processing. XP grant + building destruction + NPC cleanup + despawn + loot drop MarkVisualDirty + death SFX (PlaySfxMsg::Death with GPU position), DeathResources SystemParam with ECS queries for Home/Personality/NpcWorkState + sfx_writer + gpu_state + work_intents MessageWriter, worksite release via WorkIntentMsg(Release { uid }) — no inline entity_map.release(), fountain death triggers clear_town_roads_and_dirt + terrain/building dirty signals, HP scaling on building destruction emits SetMaxHealth + SetHealth for proper GPU normalization, tower/fountain killer branch: BuildingInstance.kills++ and .xp+=100 with level_from_xp + loot deposited directly to FoodStorage/GoldStorage + SetDamageFlash on tower), healing_system (candidate-driven: ActiveHealingSlots resource with cadenced enter-check via npcs_for_town + sustain-check with hysteresis radii, uses cache.by_faction directly (Vec<Vec<HealingZone>> indexed by faction, no per-frame HashMap), properly toggles NpcFlags.healing on/off with MarkVisualDirty when HP reaches cap or drops below), npc_regen_system (passive HP regen for NPCs with CachedStats.hp_regen > 0 from HpRegen upgrade, +0.5 HP/s per level, capped at max_health)
    behavior.rs         # Unified decision system (two-cadence top-of-loop bucket gate: COMBAT_BUCKET=16 frames for fighting NPCs, think_buckets for everything else; position hoisted into npc_pos after gate; query-first outer loop + DecisionNpcState/NpcDataQueries SystemParam bundles for per-entity mutable access, no Commands — NpcWorkState always-present, conditional writeback via original-value comparison + visual_key() dirty tracking via GpuUpdate::MarkVisualDirty, patrol route read inline without Vec clone), personality-modified flee thresholds (Brave: never flees, Coward: +20%×|m| threshold), utility AI weights from get_behavior_mods(), DecisionExtras includes gpu_updates + work_intents MessageWriters, all worksite claim/release/retarget via WorkIntentMsg (no inline entity_map.release() or try_claim_worksite()), worksite_deferred flag skips NpcWorkState write-back when messages sent, unified worksite block (Priority 5: merged Working+MiningAtMine using BuildingDef.worksite config from BUILDING_REGISTRY — shared validate/contention/drift/harvest/tired logic), arrivals simplified (~60 lines, uses Release/Retarget messages), on_duty_tick (query-first), patrol rebuild (query-first)
    work_targeting.rs   # Centralized work targeting resolver — sole owner of worksite occupancy mutations. resolve_work_targets system (MessageReader<WorkIntentMsg>, runs after decision_system in Step::Behavior): drains Claim/Release/Retarget messages, calls entity_map.release()/try_claim_worksite(), updates NpcWorkState.worksite, submits movement via PathRequestQueue, sets Activity::Idle on claim failure. find_farm_target (pub(crate), cell-ring spatial search with min-order tuple scoring) + find_mine_target (AnyTown fallback, priority-based scoring)
    economy.rs          # Game time, construction_tick_system (building construction countdown with HP scaling + sprite reveal), farm growth (iter_instances_mut with match on Farm/GoldMine, precomputed per-town yield multipliers, skips under-construction), mine regen, raider_forage_system (timer-based: accumulates hours per raider town via RaiderState.forage_timers, grants 1 food when reaching raider_forage_hours threshold, 0=disabled), mining_policy_system (auto-discovery + miner distribution), spawner_respawn_system (iterates EntityMap spawner instances), starvation_system (query-first, HP cap clamp for starving NPCs), squad_cleanup_system (query-first recruit pool, wave-aware), farm_visual_system (cadenced every 4th frame), migration spawn/attach/settle (MigrationResources SystemParam with NpcFlags + Home queries, boat spawned as proper NPC entity via SpawnNpcMsg with Job::Boat, despawn via entity+unregister_npc+free on disembark, wipeout → respawn after 4h delay, spawn-pending guard: skips wipeout check until SpawnNpcMsg processed, settlement emits TerrainDirtyMsg for tilemap rebuild)
    ai_player.rs        # AI decision system with personalities (Aggressive/Balanced/Economic), RoadStyle enum (None/Cardinal/Grid4/Grid5 — randomly assigned per town, independent of personality, persisted in save), weighted random scoring with retry loop (failed actions filtered via discriminant, re-pick from remaining), AiBuildRes SystemParam bundle, TownContext per-tick bundle (center/food/slots/mines via TownContext::build()), AiTownSnapshot caching with smart slot scoring (NeighborCounts incl. crossbow_homes, balanced_farm_ray_score, balanced_house_side_score), MineAnalysis single-pass (analyze_mines with all_positions), Option<MineAnalysis> type-safe builder-only enforcement, try_build_scored/try_build_inner/try_build_miner_home unified build helpers, waypoint_ring_slots (perimeter walk: corners always included, min 5 Manhattan spacing, skips road slots except corners), sync_patrol_perimeter_system (PerimeterSyncDirty flag-gated waypoint pruning against ideal ring, blocked slots count as covered, full destroy_building teardown), ai_squad_commander_system (wave-based attack cycle for Builder+Raider AIs, SquadUnit query, SquadRole Attack/Reserve/Idle, defense_share_pct + attack_split_weight per personality, wave gather→threshold→dispatch→retreat model, pick_raider_farm_target for raider towns, self-healing ownership scan), BuildCrossbowHome action (scored after 2+ archer homes), BuildRoads action (RoadStyle grid patterns, batch placement, Chebyshev distance ≤ 2 adjacency, grid-snapped positions), Phase 1/Phase 2 split (building + upgrade per tick, gated by build_enabled/upgrade_enabled flags), economy desire signal (slot fullness floors other desires), road+waypoint-aware slot placement (RoadStyle::is_road_slot + waypoint_ring_slots filter), debug_ai_decisions logging (failed actions with scores to last_actions), drain systems (ai_dirty_drain_system → AiSnapshotDirty, perimeter_dirty_drain_system → PerimeterSyncDirty) resolve MessageReader/Writer conflicts, Player AI Manager (faction 0 AiPlayer registered at world gen with active=false, controlled from Policies tab: enable/build/upgrade toggles + personality/road style pickers)
    audio.rs            # Music jukebox (22 tracks, random no-repeat, volume + speed control, persisted track/paused state restored on startup), SFX playback (load_sfx loads variant handles per SfxKind: ArrowShoot 1 variant from shoot.ogg, Death 24 variants from death-sounds-male-audio-pack; play_sfx_system: random variant selection, spatial camera culling BEFORE dedup (off-screen events don't consume per-kind slots), max 1 per SfxKind per frame via HashSet discriminant dedup, zero-margin viewport check (only onscreen events play) + zoom suppression at scale > 2.0)
    energy.rs           # Energy drain/recovery (drain rate multiplied by CachedStats.stamina from upgrade registry)
    sync.rs             # GPU state sync

  assets/                 # Standard Bevy asset dir (embedded in release builds via bevy_embedded_assets)
    sprites/
      roguelikeChar_transparent.png   # Character sprites (54x12 grid, 16px + 1px margin)
      roguelikeSheet_transparent.png  # World sprites (57x31 grid, 16px + 1px margin)
      heal.png                        # Heal halo sprite (single 16x16, atlas_id=2.0)
      sleep.png                       # Sleep icon sprite (single 16x16, atlas_id=3.0)
      arrow.png                       # Arrow projectile sprite (single texture, white, extras atlas col 2)
      boat.png                        # Boat migration sprite (32x32, extras atlas col 3)
      wood_walls_131x32.png            # Wall sprites (131×32 strip: E-W straight, cross, BR corner, T-junction; rotated at runtime for auto-tile)
      house.png                       # Legacy Farmer Home sprite (32x32, unused — replaced by 64x64)
      barracks.png                    # Legacy Archer Home sprite (32x32, unused — replaced by 64x64)
      waypoint.png                    # Waypoint building sprite (32x32, External tileset, upscaled to 64px)
      miner_house.png                 # Legacy Miner Home sprite (32x32, unused — replaced by 64x64)
      fighter_home.png                # Legacy Fighter Home sprite (32x32, unused — replaced by 64x64)
      farmer_home_64x64.png           # Farmer Home building sprite (64x64, External tileset)
      archer_home_64x64.png           # Archer Home building sprite (64x64, External tileset)
      crossbowman_home_64x64.png      # Crossbow Home building sprite (64x64, External tileset)
      fighter_home_64x64.png          # Fighter Home building sprite (64x64, External tileset)
      miner_home_64x64.png            # Miner Home building sprite (64x64, External tileset)
      farm_64x64.png                  # Farm building sprite (64x64, External tileset)
      merchant_64x64.png              # Merchant building sprite (64x64, External tileset)
    sounds/
      music/not-jam-music/  # 22 .ogg tracks (Not Jam Music Pack, CC0)
      sfx/shoot.ogg                         # Arrow shoot sound (ArrowShoot SFX)
      sfx/death-sounds-male-audio-pack/     # 24 .ogg death groan variants (Death SFX)
    shaders/
      npc_compute.wgsl      # WGSL compute shader (unified entity grid: NPCs + buildings, entity_flags bitmask for type differentiation, movement + spatial grid + combat targeting for NPCs + towers, tile_flags road system: speed bonus + collision bypass + attraction + wall collision: faction-aware enemy blocking)
      npc_render.wgsl       # WGSL render shader (triple vertex path: storage buffer NPC/overlay + instanced + selection brackets with procedural corner fragments at atlas_id=9, building construction clip: health < 1.0 → progressive bottom-to-top sprite reveal, health buffer pre-normalized 0.0-1.0 — no per-vertex division)
      projectile_compute.wgsl # WGSL compute shader (projectile movement + entity collision: hits both NPCs and buildings via unified spatial grid, self-collision skip via shooter index)
```
